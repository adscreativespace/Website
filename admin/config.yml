backend:
  name: git-gateway
  branch: main

media_folder: "images/uploads"
public_folder: "/images/uploads"

collections:
  - name: "projects"
    label: "Projects"
    folder: "projects"
    create: true
    slug: "{{slug}}"
    fields:
      - { label: "Project Title", name: "title", widget: "string" }
      - { label: "Category", name: "category", widget: "select", options: ["residential", "commercial", "interior", "institutional", "retail", "hospitality"] }
      - { label: "Location", name: "location", widget: "string", required: false }
      - { label: "Area (sq.ft)", name: "area", widget: "string", required: false }
      - { label: "Year", name: "year", widget: "string", required: false }
      - { label: "Short Description", name: "description", widget: "text" }
      - label: "Project Images"
        name: "images"
        widget: "list"
        field: { label: "Image", name: "image", widget: "image" }
      - label: "Card Thumbnail (pick one image to show on the card)"
        name: "thumbnail"
        widget: "image"
        required: false
